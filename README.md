# minecraft_world_parser.py

In order to run the csv generation script, you'll need `amulet-core` python library and `g++ build-essential python3-dev zlib1g-dev` on linux or `Microsoft Visual Studio C++ redistribuables` on Windows to build this library.
